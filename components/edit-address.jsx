import { InputAddress } from "@/components/input";

export default function EditAddress({ onChange }) {
  // Empty State
  // Filled State
  // Edit Modal

  return <InputAddress onChange={onChange} />;
}
